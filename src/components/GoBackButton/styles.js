import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  BtnWrapper: {
    position: "absolute",
    top: 30,
    left: 20,
  },
  Image: {
    alignSelf: "flex-end",
    width: 10,
    height: 10
  }
});
